#include <stdio.h>
#include <stdlib.h>
#include "libft.h"
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	//char *s = malloc(40 * sizeof(char));  // dynamically allocate memory
	//strcpy(s, "Work for fuck's sake!!! Really!!!");
	
	//char *t = "Blah Blah Blah, get the d.";
	//ft_memccpy(s, t, 'd', 27);
	
	//char *a = "**f**baba**abi**sik**";
	//char **plz = ft_strsplit(a, '*');
	ft_putnbr(123);
	//free(s);
	return 0;
}